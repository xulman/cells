CFG = {
    # Precise : True means no optimisations during border selection
    "precise": False,
    # Skip_step : X (int) means that during distance calculation - only every Xth pixel would be taken for comparison
    "skip_step": 5}
